{
  "name": "tacticus-api-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that relays Tacticus API calls and adds CORS headers.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.5.3",
    "wrangler": "^3.78.0"
  }
}
